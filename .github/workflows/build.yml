name: Build

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
  push:
    branches:
      - main

permissions:
  contents: read
  pull-requests: read

jobs:
  build:
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        include:
          - name: obc-native
            board: native_sim
            path: obc
          - name: obc-nucleo
            board: nucleo_g431rb
            path: obc
          - name: pay-native
            board: native_sim
            path: pay
          - name: pay-nucleo
            board: nucleo_h743zi
            path: pay

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          chmod +x ./scripts/install_dependencies.sh
          ./scripts/install_dependencies.sh

      - name: Setup Python venv
        run: |
          chmod +x ./scripts/setup_python_venv.sh
          ./scripts/setup_python_venv.sh

      - name: Setup west workspace
        run: |
          chmod +x ./scripts/setup_west_workspace.sh
          ./scripts/setup_west_workspace.sh

      - name: Build app
        run: |
          source ../.venv/bin/activate
          source ../zephyr/zephyr-env.sh
          APP_PATH="./apps/${{ matrix.path }}"
          BOARD_PATH="../../board/${{ matrix.path }}/${{ matrix.board }}.overlay"
          west -v build -b ${{ matrix.board }} $APP_PATH -p -- -DOVERLAY_CONFIG=$BOARD_PATH
